{
	"private": true,
	"version": "0.0.0",
	"scripts": {
		"deploy": "wrangler publish index.js",
		"dev": "wrangler dev index.js --local",
		"test": "vitest"
	},
	"devDependencies": {
		"vitest": "^4.1.9",
		"wrangler": "^4.104.0"
	}
}
