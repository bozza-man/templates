{
	"private": true,
	"version": "0.0.0",
	"scripts": {
		"deploy": "wrangler publish index.js",
		"dev": "wrangler dev index.js --local",
		"test": "vitest"
	},
	"devDependencies": {
		"vitest": "^4.1.8",
		"wrangler": "^4.96.0"
	}
}
